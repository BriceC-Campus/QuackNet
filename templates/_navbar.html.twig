<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('app_quack_index') }}">Quack Net</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01"
                aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarColor01">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link active" href="{{ path('app_quack_index') }}">Quack
                        <span class="visually-hidden">(current)</span>
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_duck_index') }}">Duck</a>
                </li>
                {% if not is_granted('IS_AUTHENTICATED') %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_register') }}">Sign Up</a>
                    </li>
                {% endif %}
            </ul>
            {% if is_granted('IS_AUTHENTICATED') %}
                <div class="me-3 d-inline-flex align-items-center">
                    <a class="text-light me-3" href="{{ path('app_logout') }}">Log out</a>
                    <h4 class="text-light mb-0">{{ app.user.duckname }}</h4>
                </div>
            {% else %}
                <div class="me-3">
                    <a class="text-light" href="{{ path('app_login') }}">Login</a>
                </div>
            {% endif %}
        </div>
    </div>
</nav>